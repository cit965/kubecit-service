// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"kubecit-service/ent/predicate"
	"kubecit-service/ent/vipproduct"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// VipProductQuery is the builder for querying VipProduct entities.
type VipProductQuery struct {
	config
	ctx        *QueryContext
	order      []vipproduct.OrderOption
	inters     []Interceptor
	predicates []predicate.VipProduct
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VipProductQuery builder.
func (vpq *VipProductQuery) Where(ps ...predicate.VipProduct) *VipProductQuery {
	vpq.predicates = append(vpq.predicates, ps...)
	return vpq
}

// Limit the number of records to be returned by this query.
func (vpq *VipProductQuery) Limit(limit int) *VipProductQuery {
	vpq.ctx.Limit = &limit
	return vpq
}

// Offset to start from.
func (vpq *VipProductQuery) Offset(offset int) *VipProductQuery {
	vpq.ctx.Offset = &offset
	return vpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vpq *VipProductQuery) Unique(unique bool) *VipProductQuery {
	vpq.ctx.Unique = &unique
	return vpq
}

// Order specifies how the records should be ordered.
func (vpq *VipProductQuery) Order(o ...vipproduct.OrderOption) *VipProductQuery {
	vpq.order = append(vpq.order, o...)
	return vpq
}

// First returns the first VipProduct entity from the query.
// Returns a *NotFoundError when no VipProduct was found.
func (vpq *VipProductQuery) First(ctx context.Context) (*VipProduct, error) {
	nodes, err := vpq.Limit(1).All(setContextOp(ctx, vpq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{vipproduct.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vpq *VipProductQuery) FirstX(ctx context.Context) *VipProduct {
	node, err := vpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first VipProduct ID from the query.
// Returns a *NotFoundError when no VipProduct ID was found.
func (vpq *VipProductQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vpq.Limit(1).IDs(setContextOp(ctx, vpq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{vipproduct.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vpq *VipProductQuery) FirstIDX(ctx context.Context) int {
	id, err := vpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single VipProduct entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one VipProduct entity is found.
// Returns a *NotFoundError when no VipProduct entities are found.
func (vpq *VipProductQuery) Only(ctx context.Context) (*VipProduct, error) {
	nodes, err := vpq.Limit(2).All(setContextOp(ctx, vpq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{vipproduct.Label}
	default:
		return nil, &NotSingularError{vipproduct.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vpq *VipProductQuery) OnlyX(ctx context.Context) *VipProduct {
	node, err := vpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only VipProduct ID in the query.
// Returns a *NotSingularError when more than one VipProduct ID is found.
// Returns a *NotFoundError when no entities are found.
func (vpq *VipProductQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vpq.Limit(2).IDs(setContextOp(ctx, vpq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{vipproduct.Label}
	default:
		err = &NotSingularError{vipproduct.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vpq *VipProductQuery) OnlyIDX(ctx context.Context) int {
	id, err := vpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of VipProducts.
func (vpq *VipProductQuery) All(ctx context.Context) ([]*VipProduct, error) {
	ctx = setContextOp(ctx, vpq.ctx, "All")
	if err := vpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*VipProduct, *VipProductQuery]()
	return withInterceptors[[]*VipProduct](ctx, vpq, qr, vpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (vpq *VipProductQuery) AllX(ctx context.Context) []*VipProduct {
	nodes, err := vpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of VipProduct IDs.
func (vpq *VipProductQuery) IDs(ctx context.Context) (ids []int, err error) {
	if vpq.ctx.Unique == nil && vpq.path != nil {
		vpq.Unique(true)
	}
	ctx = setContextOp(ctx, vpq.ctx, "IDs")
	if err = vpq.Select(vipproduct.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vpq *VipProductQuery) IDsX(ctx context.Context) []int {
	ids, err := vpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vpq *VipProductQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, vpq.ctx, "Count")
	if err := vpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, vpq, querierCount[*VipProductQuery](), vpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (vpq *VipProductQuery) CountX(ctx context.Context) int {
	count, err := vpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vpq *VipProductQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, vpq.ctx, "Exist")
	switch _, err := vpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (vpq *VipProductQuery) ExistX(ctx context.Context) bool {
	exist, err := vpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VipProductQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vpq *VipProductQuery) Clone() *VipProductQuery {
	if vpq == nil {
		return nil
	}
	return &VipProductQuery{
		config:     vpq.config,
		ctx:        vpq.ctx.Clone(),
		order:      append([]vipproduct.OrderOption{}, vpq.order...),
		inters:     append([]Interceptor{}, vpq.inters...),
		predicates: append([]predicate.VipProduct{}, vpq.predicates...),
		// clone intermediate query.
		sql:  vpq.sql.Clone(),
		path: vpq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Price float64 `json:"price,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.VipProduct.Query().
//		GroupBy(vipproduct.FieldPrice).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (vpq *VipProductQuery) GroupBy(field string, fields ...string) *VipProductGroupBy {
	vpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &VipProductGroupBy{build: vpq}
	grbuild.flds = &vpq.ctx.Fields
	grbuild.label = vipproduct.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Price float64 `json:"price,omitempty"`
//	}
//
//	client.VipProduct.Query().
//		Select(vipproduct.FieldPrice).
//		Scan(ctx, &v)
func (vpq *VipProductQuery) Select(fields ...string) *VipProductSelect {
	vpq.ctx.Fields = append(vpq.ctx.Fields, fields...)
	sbuild := &VipProductSelect{VipProductQuery: vpq}
	sbuild.label = vipproduct.Label
	sbuild.flds, sbuild.scan = &vpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a VipProductSelect configured with the given aggregations.
func (vpq *VipProductQuery) Aggregate(fns ...AggregateFunc) *VipProductSelect {
	return vpq.Select().Aggregate(fns...)
}

func (vpq *VipProductQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range vpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, vpq); err != nil {
				return err
			}
		}
	}
	for _, f := range vpq.ctx.Fields {
		if !vipproduct.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vpq.path != nil {
		prev, err := vpq.path(ctx)
		if err != nil {
			return err
		}
		vpq.sql = prev
	}
	return nil
}

func (vpq *VipProductQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*VipProduct, error) {
	var (
		nodes = []*VipProduct{}
		_spec = vpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*VipProduct).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &VipProduct{config: vpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(vpq.modifiers) > 0 {
		_spec.Modifiers = vpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (vpq *VipProductQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vpq.querySpec()
	if len(vpq.modifiers) > 0 {
		_spec.Modifiers = vpq.modifiers
	}
	_spec.Node.Columns = vpq.ctx.Fields
	if len(vpq.ctx.Fields) > 0 {
		_spec.Unique = vpq.ctx.Unique != nil && *vpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, vpq.driver, _spec)
}

func (vpq *VipProductQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(vipproduct.Table, vipproduct.Columns, sqlgraph.NewFieldSpec(vipproduct.FieldID, field.TypeInt))
	_spec.From = vpq.sql
	if unique := vpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if vpq.path != nil {
		_spec.Unique = true
	}
	if fields := vpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, vipproduct.FieldID)
		for i := range fields {
			if fields[i] != vipproduct.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vpq *VipProductQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vpq.driver.Dialect())
	t1 := builder.Table(vipproduct.Table)
	columns := vpq.ctx.Fields
	if len(columns) == 0 {
		columns = vipproduct.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vpq.sql != nil {
		selector = vpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vpq.ctx.Unique != nil && *vpq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range vpq.modifiers {
		m(selector)
	}
	for _, p := range vpq.predicates {
		p(selector)
	}
	for _, p := range vpq.order {
		p(selector)
	}
	if offset := vpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (vpq *VipProductQuery) ForUpdate(opts ...sql.LockOption) *VipProductQuery {
	if vpq.driver.Dialect() == dialect.Postgres {
		vpq.Unique(false)
	}
	vpq.modifiers = append(vpq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return vpq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (vpq *VipProductQuery) ForShare(opts ...sql.LockOption) *VipProductQuery {
	if vpq.driver.Dialect() == dialect.Postgres {
		vpq.Unique(false)
	}
	vpq.modifiers = append(vpq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return vpq
}

// VipProductGroupBy is the group-by builder for VipProduct entities.
type VipProductGroupBy struct {
	selector
	build *VipProductQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vpgb *VipProductGroupBy) Aggregate(fns ...AggregateFunc) *VipProductGroupBy {
	vpgb.fns = append(vpgb.fns, fns...)
	return vpgb
}

// Scan applies the selector query and scans the result into the given value.
func (vpgb *VipProductGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vpgb.build.ctx, "GroupBy")
	if err := vpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VipProductQuery, *VipProductGroupBy](ctx, vpgb.build, vpgb, vpgb.build.inters, v)
}

func (vpgb *VipProductGroupBy) sqlScan(ctx context.Context, root *VipProductQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(vpgb.fns))
	for _, fn := range vpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*vpgb.flds)+len(vpgb.fns))
		for _, f := range *vpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*vpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// VipProductSelect is the builder for selecting fields of VipProduct entities.
type VipProductSelect struct {
	*VipProductQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (vps *VipProductSelect) Aggregate(fns ...AggregateFunc) *VipProductSelect {
	vps.fns = append(vps.fns, fns...)
	return vps
}

// Scan applies the selector query and scans the result into the given value.
func (vps *VipProductSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vps.ctx, "Select")
	if err := vps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VipProductQuery, *VipProductSelect](ctx, vps.VipProductQuery, vps, vps.inters, v)
}

func (vps *VipProductSelect) sqlScan(ctx context.Context, root *VipProductQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(vps.fns))
	for _, fn := range vps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*vps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
