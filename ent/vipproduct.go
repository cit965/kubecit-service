// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"kubecit-service/ent/vipproduct"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// VipProduct is the model entity for the VipProduct schema.
type VipProduct struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 会员价格
	Price float64 `json:"price,omitempty"`
	// 会员名称
	Name string `json:"name,omitempty"`
	// 会员描述
	Description  string `json:"description,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VipProduct) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case vipproduct.FieldPrice:
			values[i] = new(sql.NullFloat64)
		case vipproduct.FieldID:
			values[i] = new(sql.NullInt64)
		case vipproduct.FieldName, vipproduct.FieldDescription:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VipProduct fields.
func (vp *VipProduct) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case vipproduct.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			vp.ID = int(value.Int64)
		case vipproduct.FieldPrice:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price", values[i])
			} else if value.Valid {
				vp.Price = value.Float64
			}
		case vipproduct.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				vp.Name = value.String
			}
		case vipproduct.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				vp.Description = value.String
			}
		default:
			vp.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the VipProduct.
// This includes values selected through modifiers, order, etc.
func (vp *VipProduct) Value(name string) (ent.Value, error) {
	return vp.selectValues.Get(name)
}

// Update returns a builder for updating this VipProduct.
// Note that you need to call VipProduct.Unwrap() before calling this method if this VipProduct
// was returned from a transaction, and the transaction was committed or rolled back.
func (vp *VipProduct) Update() *VipProductUpdateOne {
	return NewVipProductClient(vp.config).UpdateOne(vp)
}

// Unwrap unwraps the VipProduct entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (vp *VipProduct) Unwrap() *VipProduct {
	_tx, ok := vp.config.driver.(*txDriver)
	if !ok {
		panic("ent: VipProduct is not a transactional entity")
	}
	vp.config.driver = _tx.drv
	return vp
}

// String implements the fmt.Stringer.
func (vp *VipProduct) String() string {
	var builder strings.Builder
	builder.WriteString("VipProduct(")
	builder.WriteString(fmt.Sprintf("id=%v, ", vp.ID))
	builder.WriteString("price=")
	builder.WriteString(fmt.Sprintf("%v", vp.Price))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(vp.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(vp.Description)
	builder.WriteByte(')')
	return builder.String()
}

// VipProducts is a parsable slice of VipProduct.
type VipProducts []*VipProduct
